# Repo memory: graph

> Auto-generated by repo-memory — commit this file to share with your team.
> Add domain corrections in `.agent-memory/overrides/`.

## How to use this memory

Before starting any new task, call `memory_prepare_context_tool` with the task description. It returns the relevant files, tests, and rules so you go straight to the right place without exploring the codebase blind.

Available tools:
- `memory_prepare_context_tool <task>` — focused file + test + rule pack for a task
- `memory_explain_area_tool <area>` — explain a feature, module, or file path
- `memory_recent_changes_tool <area>` — what changed recently in an area

## Purpose

Every Claude Code session starts cold. You paste architecture context. You re-explain the file layout. You repeat what you said last week.

## Stack

**Languages:** python, typescript
**Source dirs:** code_review_graph
**Test dirs:** code-review-graph-vscode/test, tests
**Tooling dirs:** benchmarks, evaluate

## Top-level layout

- `benchmarks/`
- `code-review-graph-vscode/`
- `code_review_graph/`
- `diagrams/`
- `docs/`
- `evaluate/`
- `hooks/`
- `skills/`
- `tests/`

## Key conventions

- **python**: Follow PEP 8 style (4-space indent, max 79–100 chars per line).
- **python**: Use type annotations on all public functions.
- **typescript**: Use strict TypeScript (`strict: true` in tsconfig).
- **typescript**: Prefer `interface` over `type` for object shapes.

## Entry points

**CLI commands:** `repomind`
